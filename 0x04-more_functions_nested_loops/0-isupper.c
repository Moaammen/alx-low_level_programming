#include "main.h"

/**
 * _isupper - function
 * Description: test for uppercase character
 * @c: integer
 * Return: 1 if uppercase 0 if not
 */

int _isupper(int c)
{
if (c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}
